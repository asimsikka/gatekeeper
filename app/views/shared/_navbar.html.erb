<nav class="bg-white shadow-sm border-b border-gray-200">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16 items-center">
      <div class="flex items-center">
        <%= link_to "Gatekeeper", root_path, class: "text-xl font-bold text-blue-600" %>
      </div>

      <div class="flex items-center space-x-4">
        <% if user_signed_in? %>
          <span class="text-gray-700 text-sm"><%= current_user.email %></span>
          <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "text-sm text-red-600 hover:text-red-700 font-medium" %>
        <% else %>
          <%= link_to "Sign In", new_user_session_path, class: "text-sm text-gray-600 hover:text-blue-600" %>
          <%= link_to "Sign Up", new_user_registration_path, class: "text-sm text-blue-600 hover:text-blue-700 font-semibold" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>
