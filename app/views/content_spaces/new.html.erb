<div class="min-h-screen flex items-center justify-center bg-gray-50 px-4 py-12">
  <div class="w-full max-w-xl bg-white rounded-xl shadow-md p-8 space-y-2">
    <h1 class="text-2xl font-bold mb-6 text-gray-800">
      <%= @content_space.persisted? ? "Edit Content Space" : "New Content Space" %>
    </h1>
    <%= render "form" %>
    <div class="mt-4 text-center">
      <%= link_to "← Back to Spaces", organization_content_spaces_path(@organization),
                  class: "text-sm text-gray-600 hover:text-blue-600" %>
    </div>
  </div>
</div>
