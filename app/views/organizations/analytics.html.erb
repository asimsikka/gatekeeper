<h2 class="text-2xl font-bold mb-4">Analytics for <%= @organization.name %></h2>

<div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
  <div class="p-4 bg-white shadow rounded">
    <h3 class="text-lg font-semibold">Total Members</h3>
    <p><%= @memberships.count %></p>
  </div>
  <div class="p-4 bg-white shadow rounded">
    <h3 class="text-lg font-semibold">Admins</h3>
    <p><%= @admin_count %></p>
  </div>
  <div class="p-4 bg-white shadow rounded">
    <h3 class="text-lg font-semibold">Recently Joined</h3>
    <ul>
      <% @recent_members.each do |membership| %>
        <li><%= membership.user.email %> — <%= membership.created_at.to_date %></li>
      <% end %>
    </ul>
  </div>
</div>
