<div class="min-h-screen bg-gray-50 py-12 px-4 sm:px-6 lg:px-8">
  <div class="max-w-6xl mx-auto">
    <div class="flex items-center justify-between mb-8">
      <h2 class="text-3xl font-bold text-gray-800 align-top">
        Members of <span class="text-blue-600"><%= @organization.name %></span>
      </h2>
      <div class="flex flex-col items-end space-y-2">
        <%= link_to "← Back", analytics_organization_path(@organization),
                    class: "text-sm text-gray-600 hover:text-gray-900" %>
        <%= link_to "Invite Member", new_organization_membership_path(@organization),
                    class: "bg-green-500 text-white px-4 py-2 rounded shadow hover:bg-green-600 transition" %>
      </div>
    </div>
    <div class="p-6 bg-white rounded-xl border border-gray-200 shadow-sm">
      <%= render partial: "member_details", locals: { memberships: @memberships, member: true } %>
    </div>
  </div>
</div>
